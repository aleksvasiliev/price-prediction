{
  "name": "web-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd ../.. && pnpm install --no-frozen-lockfile && pnpm --filter web build",
    "start": "serve dist -s -p $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
